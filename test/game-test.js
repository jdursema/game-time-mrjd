const chai = require('chai');
const assert = chai.assert;
const game = require('../lib/game.js');

describe('Game', () => {
  let game;

  beforeEach( () => {
    game = new Game();
  })

  it('should have a game array', () => {
    assert.equal(game.gameArray, )
  })


                   // const lastPlayer = this.gameArray[5][0].player
                   // assert.equal(lastPlayer, 1)
})